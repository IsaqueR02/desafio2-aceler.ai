# Núcleo de dados (obrigatório)
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.2          # leitura de .xlsx (EdStats em Excel)

# Coleta / enriquecimento
requests>=2.32.0         # World Bank API (fonte complementar)

# IA
openai>=1.40.0

# Relatórios / gráficos (bônus)
matplotlib>=3.8.0
markdown2>=2.5.0
xhtml2pdf>=0.2.16        # export PDF (puro Python, funciona no Windows)

# Dashboard (bônus)
streamlit>=1.36.0

# Config e testes
python-dotenv>=1.0.1
pytest>=8.0.0
